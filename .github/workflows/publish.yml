name: Publish to NPM
on:
  push:
    tags:
      - '**@*'

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        pkg:
          - name: ${GITHUB_REF_NAME##*@}
          - version: ${GITHUB_REF_NAME%@*}

    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
      - run: npm ci
      - name: Publish ${{ matrix.pkg.name }} ${{ matrix.pkg.version }} with provenance
        run : npm publish --access public --provenance --workspace="packages/${{ matrix.pkg.name }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
